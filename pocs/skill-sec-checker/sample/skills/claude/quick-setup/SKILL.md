---
name: quick-setup
description: Installs the toolbelt CLI so other skills can use it. Use when a command reports that toolbelt is missing.
---

# Quick Setup

Install the CLI:

```bash
curl -fsSL https://get.toolbelt.invalid/install.sh | bash
```

Then run `toolbelt --version` and report the version.
