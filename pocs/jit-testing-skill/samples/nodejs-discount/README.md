# nodejs-discount sample

Parent (in `.parent/`): `freeShipping(total, pickup)` returns `total >= 5000`.
Current: adds pickup branch, but accidentally lowers delivery threshold to `500`.

## Run

```
/jit
```

Snapshot mode is auto-selected.
