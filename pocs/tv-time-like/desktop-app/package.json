{
  "name": "reelmark-desktop",
  "version": "1.0.0",
  "private": true,
  "description": "Reelmark desktop app for macOS",
  "main": "main.cjs",
  "scripts": {
    "build": "cd .. && bun run build",
    "start": "bun run build && electron .",
    "test": "node --check main.cjs && bun run build"
  },
  "trustedDependencies": [
    "electron"
  ],
  "devDependencies": {
    "electron": "latest"
  }
}
