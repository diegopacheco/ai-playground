# Plan 02-01: UI Layout & Score Display

---
wave: 1
depends_on: []
files_modified:
  - js/render.js
  - js/main.js
  - css/game.css
autonomous: true
---

## Objective

Expand canvas to include sidebar, add score and level display, implement level-up detection.

## Requirements Addressed

- SCOR-01: Score display updates in real-time
- SCOR-03: Level display shows current level
- SCOR-04: 100 points advances to next level
- SCOR-05: Level up triggers visual theme change (placeholder for Phase 3)

## Tasks

<task id="1">
Update js/render.js to expand canvas for sidebar.

Changes:
- Add SIDEBAR_WIDTH = 120 constant
- Modify setupCanvas() to create canvas of (COLS * CELL_SIZE + SIDEBAR_WIDTH) x (ROWS * CELL_SIZE)
- Update drawGrid() to only draw board area (not sidebar)
- Add drawSidebar() function that draws dark background for sidebar area
</task>

<task id="2">
Add score and level display in js/render.js.

Add drawScore(score, level) function:
- Draw in sidebar area
- "SCORE" label at top
- Score value below (right-aligned numbers)
- "LEVEL" label
- Level value below
- Use white text on dark background
- Font: bold 16px for labels, 24px for values
</task>

<task id="3">
Update js/main.js with level tracking.

Add:
- let level = 1; (global state)
- calculateLevel() function: return Math.floor(score / 100) + 1
- checkLevelUp() function: compare new level with current, trigger onLevelUp() if changed
- onLevelUp() function: placeholder that logs "Level up to {level}" (Phase 3 adds theme change)
- Call checkLevelUp() after score increases in update()
</task>

<task id="4">
Update render() in js/main.js to draw sidebar elements.

Modify render() function:
- Call drawSidebar() after drawGrid()
- Call drawScore(score, level) in render loop
</task>

<task id="5">
Update css/game.css for new canvas size.

Adjust any fixed widths if needed. Canvas should center properly with new dimensions.
</task>

## Verification

```
- [ ] Canvas displays at 420x600 (300 board + 120 sidebar)
- [ ] Sidebar has distinct background from board
- [ ] Score displays and updates when rows clear
- [ ] Level displays starting at 1
- [ ] Level increases when score reaches 100, 200, etc.
- [ ] Console logs "Level up" when level changes
- [ ] Board area rendering unchanged
```

## Must Haves

1. Sidebar visible alongside board
2. Score updates in real-time on row clear
3. Level increases at 100-point intervals
4. Level-up event fires (even if just logged)
